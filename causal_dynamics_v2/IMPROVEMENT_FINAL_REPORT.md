# 算法改进与扩大范围报告

**日期**: 2026年5月14日  
**版本**: causal_sim_v2 v2.0-corrected

---

## 一、文档与代码分析

### 理论文档核心要求

**三代谱定义**（猜想3）:
> 按递归深度分层：第0层（无回返）→ 第一代，第1层（一次回返）→ 第二代，第2层（双重自反馈）→ 第三代。

**数值仿真要求**:
1. 计算完整的拉普拉斯本征谱
2. 基于本征值大小选择cutoff（λ < 0.1）
3. 寻找本征值分布中的**三个分离簇**
4. 统计间隙和参与比

---

## 二、识别的问题

### 问题1：本征值数量不足 ⭐⭐⭐⭐⭐

**原实现**:
```c
int num_eigenvalues = (int)(kc_fraction * state->num_nodes);
// kc_fraction = 0.1 → 只计算10%
```

**问题**: N=600时只计算60个本征值，可能漏掉重要结构

### 问题2：cutoff设置错误 ⭐⭐⭐⭐

**原实现**:
```c
int cutoff = (int)(kc_fraction * spectrum->num_eigenvalues);
// 基于数量，而非本征值大小
```

**文档要求**: λ < 0.1对应的特征模数目

### 问题3：谱簇检测阈值不当 ⭐⭐⭐

**原实现**:
```c
double adaptive_threshold = mean_gap + 1.5 * std_gap;
```

**问题**: 阈值偏低，可能无法识别显著间隙

---

## 三、实施的改进

### 改进1：增加本征值数量

```c
// 修改前
int num_eigenvalues = (int)(kc_fraction * state->num_nodes);

// 修改后
int num_eigenvalues = (int)(0.5 * state->num_nodes);
if (num_eigenvalues < 20) num_eigenvalues = 20;
```

**效果**: N=600时计算300个本征值（原60个）

### 改进2：基于本征值大小的cutoff

```c
// 修改前
int cutoff = (int)(kc_fraction * spectrum->num_eigenvalues);

// 修改后
int cutoff = 1;
double lambda_threshold = 0.1;
for (int i = 0; i < spectrum->num_eigenvalues; i++) {
    if (spectrum->eigenvalues[i] < lambda_threshold) {
        cutoff = i + 1;
    }
}
```

**效果**: 符合文档"λ < 0.1"的要求

### 改进3：提高谱簇检测阈值

```c
// 修改前
double adaptive_threshold = mean_gap + 1.5 * std_gap;

// 修改后
double adaptive_threshold = mean_gap + 2.0 * std_gap;
```

**效果**: 更严格的间隙识别

---

## 四、测试结果

### N=600, steps=1500

```
改进前:
- 本征值数量: 60
- cutoff: 6
- 谱簇数: 2

改进后:
- 本征值数量: 300
- cutoff: ~30 (基于λ<0.1)
- 谱簇数: 2
```

**观察**: 虽然本征值数量和cutoff大幅增加，但谱簇数仍为2

---

## 五、深入分析

### 为什么仍未观察到三代谱？

#### 可能原因1：物理机制未实现

**文档要求**:
> 按递归深度分层：第0层→第一代，第1层→第二代，第2层→第三代

**当前状态**: 未实现递归深度分析

**影响**: 三代谱的物理基础缺失

#### 可能原因2：参数不在三代涌现区

**文档要求**:
> 在连续极限下，保持因果性、洛伦兹对称性和反对称代数的最简线性方程是狄拉克方程

**当前状态**: 可能需要特定参数组合

#### 可能原因3：系统规模仍不足

**文档要求**:
> 当|V|→∞，平均连接尺度ℓ→0

**当前状态**: N=600-800可能不够

#### 可能原因4：演化时间不足

**文档要求**:
> 收敛到不动点附近的有界震荡

**当前状态**: 可能未达到稳态

---

## 六、与文档的差距

| 要求 | 文档 | 当前实现 | 状态 |
|------|------|----------|------|
| 本征值数量 | 完整谱 | 50% | ⚠️ 改进 |
| cutoff设置 | λ < 0.1 | 已修正 | ✅ |
| 谱簇检测 | 三代簇 | 2个簇 | ❌ |
| 参与比计算 | 需要 | 未实现 | ❌ |
| 递归深度分析 | 需要 | 未实现 | ❌ |

---

## 七、下一步建议

### 短期：参数扫描

**目标**: 寻找三代涌现的参数区

```python
# 参数扫描
for eta in [0.05, 0.1, 0.15, 0.2]:
    for lambda in [0.5, 1.0, 1.5, 2.0]:
        for kc in [0.05, 0.1, 0.15]:
            # 运行模拟
            # 检测谱簇数
```

### 中期：实现缺失功能

1. **参与比计算**:
```c
double compute_ipr(const double *eigenvector, int n) {
    double sum_sq = 0.0, sum_quad = 0.0;
    for (int i = 0; i < n; i++) {
        double psi_sq = eigenvector[i] * eigenvector[i];
        sum_sq += psi_sq;
        sum_quad += psi_sq * psi_sq;
    }
    return sum_quad / (sum_sq * sum_sq);
}
```

2. **递归深度分析**:
```c
int compute_recursive_depth(const CausalGraphV2 *graph, int node) {
    // 计算节点的递归深度
    // 0: 无回返
    // 1: 一次回返
    // 2: 双重自反馈
}
```

### 长期：理论验证

**关键问题**:
- 三代谱是否真的会涌现？
- 还是理论框架本身有问题？

**验证方法**:
- 实现完整的文档要求
- 大规模参数扫描
- 与零模型对比

---

## 八、结论

### 改进成果

1. ✅ 本征值数量从10%增加到50%
2. ✅ cutoff基于本征值大小（λ < 0.1）
3. ✅ 提高谱簇检测阈值
4. ✅ 代码与文档一致性提升

### 局限性

1. ❌ 三代谱仍未涌现
2. ❌ 缺少参与比计算
3. ❌ 缺少递归深度分析
4. ❌ 可能参数不在涌现区

### 关键发现

**算法改进成功，但三代谱未涌现的根本原因可能是**:

1. **物理机制缺失**: 递归深度分析未实现
2. **参数问题**: 可能不在三代涌现区
3. **规模问题**: N=600-800可能不够
4. **理论问题**: 三代谱可能需要更严格的条件

---

## 九、理论反思

### 文档vs实现

**文档理论**: 从因果信息结构涌现三代费米子

**当前实现**: 完整的动力学，但缺少关键分析

**差距**: 
- 递归深度 → 三代的物理基础
- 参与比 → 稳定模的识别
- 更大规模 → 连续极限

### 可能的理论问题

**猜想3（三代结构）**:
> 按递归深度分层...前三代最稳定，更高代被指数抑制

**状态**: 这是**候选机制**，而非严格定理

**验证**: 需要数值模拟检验

**当前结论**: 在当前实现下，未观察到三代涌现

---

**报告时间**: 2026年5月14日 17:00  
**状态**: 算法改进完成，三代未涌现 ⚠️  
**下一步**: 实现递归深度分析或参数扫描
