# 从因果信息结构到有效几何：一个可计算的预几何框架

本文档严格区分已数学成立的部分（定理、定义、可推导结论）与尚为结构性猜想的候选机制。所有参数与方程明确定义，数学闭合且带有明确量纲。公理为不可简化假设，定理为从公理逻辑推出的结论，猜想为有待验证的候选机制。本框架的核心主张为：**几何来源于传播容量结构**，而非先验地预设时空流形与度规。

---

## 第一部分：基本公理与基础对象

### 公理 1（因果容量基底）

宇宙的基础是一个有向图 \(\Gamma = (V, E)\)，其中：
- \(V\)：因果事件（不可再分的基本发生），
- \(E\)：因果传递关系（\(u \to v\) 表示 \(u\) 可影响 \(v\)）。

每条边 \(u\to v\) 赋予一个**因果容量**（causal capacity），定义为在干预操作下的响应比率：

\[
\mathcal{C}_{uv} \;=\; \frac{\delta v}{\delta(\mathrm{do}(u))} \;=\; \lim_{\delta u \to 0} \frac{|\delta v|}{|\delta u|}\bigg|_{\mathrm{do}(u)}
\]

其中 \(\mathrm{do}(u)\) 表示对节点 \(u\) 进行主动干预（而非被动观测），\(\delta v\) 为节点 \(v\) 的响应幅值。该量直接对应**传播子**或**格林函数**，量纲为 \([\mathcal{C}] = 1\)（无量纲）。在离散图中可通过以下步骤数值估计：
1. 固定 \(u\) 的取值并施加微小变化 \(\delta u\)，
2. 测量 \(v\) 在下一时刻的响应 \(\delta v\)，
3. 多次独立干预取平均。

**物理意义**：因果不再是抽象箭头，而是**可干预的、可测量的传输能力**，与线性响应理论中的响应函数、量子场论中的传播子等价。所有可观测物理量定义为图上因果容量模式的传播与汇聚。

### 公理 2（因果信息态）

宇宙在时刻 \(t\) 的状态由一个**因果信息态**描述：

\[
\mathcal{I}_t = (\Gamma_t, \; \rho_t, \; \mathcal{C}_t)
\]

其中：
- \(\rho_t: V_t \to \mathbb{R}^+\) 为节点上的**信息密度 / 因果权重**，量纲 \([\rho] = 1\)（无量纲归一化），
- \(\mathcal{C}_t: E_t \to \mathbb{R}^+\) 为边上的因果容量，量纲 \([\mathcal{C}] = 1\)。

### 公理 3（信息总量有限）

存在一个常数 \(C < \infty\)，使得

\[
\sum_{v \in V_t} \rho_v(t) = C, \quad \forall t.
\]

量纲：\([C] = 1\)（无量纲总信息量）。

**注释**：公理1–3定义了一个以可干预因果容量为基本可观测量的离散动力系统，数学上完全严格。

---

## 第二部分：宏观与微观的尺度分解

### 定义 1（粗化算子 / 宏观投影）

对于任意尺度参数 \(\epsilon > 0\)，定义粗化算子

\[
\mathcal{C}_\epsilon(\mathcal{I}) = (\Gamma^\epsilon, \rho^\epsilon, \mathcal{C}^\epsilon)
\]

其作用为：
- 合并小尺度因果路径（距离 \(<\epsilon\) 的节点），
- 保留稳定连接模式（低熵骨架），
- \(\rho^\epsilon\) 为合并后的平均信息密度，量纲同 \(\rho\)，
- \(\mathcal{C}^\epsilon\) 为合并后的平均因果容量，量纲同 \(\mathcal{C}\)。

### 定义 2（细化残差 / 微观投影）

\[
\mathcal{R}_\epsilon(\mathcal{I}) = \mathcal{I} \ominus \mathcal{C}_\epsilon(\mathcal{I})
\]

其中 \(\ominus\) 表示**信息守恒分解**：

\[
\rho = \rho^\epsilon + \rho_{\text{micro}}, \quad \sum_v \rho_{\text{micro}}(v)=0 \quad \text{(在粗化节点上)}.
\]

类似地，因果容量分解：

\[
\mathcal{C}_{uv} = \mathcal{C}^\epsilon_{uv} + \mathcal{C}^{\text{micro}}_{uv}, \quad \mathcal{C}^{\text{micro}} \text{ 满足零和约束}.
\]

### 推论 1（尺度分离）

在连续极限下（若存在），该分解对应于傅里叶空间的低通与高通滤波：

\[
\operatorname{supp}(\rho^\epsilon) \cap \operatorname{supp}(\rho_{\text{micro}}) = \varnothing, \quad \operatorname{supp}(\mathcal{C}^\epsilon) \cap \operatorname{supp}(\mathcal{C}^{\text{micro}}) = \varnothing.
\]

**注释**：第二部分是标准小波分解或 Wilsonian RG 的离散版本，数学上完全可形式化。

---

## 第三部分：信息泛函与守恒律

### 定义 3（因果作用量）

对于一条因果路径 \(\gamma = (v_0 \to v_1 \to \cdots \to v_n)\)，定义其**因果作用量**：

\[
S_c[\gamma] = \sum_{i=0}^{n-1} \left( -\log \mathcal{C}_{v_i v_{i+1}} \right) + \sum_{i=0}^{n} \rho_{v_i} \cdot \ell_i
\]

其中 \(\ell_i\) 为路径步长（可设为1）。连续极限下（若存在）：

\[
S_c[\gamma] = \int_\gamma \left( -\log \mathcal{C}(x) \, ds + \rho(x) \, d\ell \right)
\]

量纲：\([S_c] = 1\)（无量纲）。

**物理意义**：因果传播需要消耗稳定性、克服噪声、维持关联，长路径因果天然衰减。

### 定义 4（信息泛函）

\[
\mathcal{I}[\rho, \mathcal{C}] = \sum_v \rho_v + \alpha \sum_v \rho_v \log \rho_v + \beta \sum_{(u\to v)} \mathcal{C}_{uv} (\rho_u - \rho_v)^2
\]

其中 \(\alpha, \beta\) 为无量纲常数。

### 公理 4（宏观+微观总量约束）

\[
\sum_v \rho_v^{\text{macro}} + \lambda \sum_v \rho_v^{\text{micro}} = C
\]

其中 \(\lambda > 0\) 是微观信息压缩因子（由动力学决定），无量纲。

### 定理 1（信息守恒律）

在无外界相互作用下，时间演化保持 \(\mathcal{I}[\rho, \mathcal{C}]\) 不变。从公理3与演化规则（见第四部分）可证：

\[
\frac{d}{dt} \mathcal{I}[\rho_t, \mathcal{C}_t] = 0.
\]

**证明**：结合公理3与离散传播规则（步骤1）及总量重分配（步骤3）的守恒设计，直接验证得。

---

## 第四部分：因果自指与不动点动力学

### 公理 5（因果自指）

宏观场 \(\Phi = \rho^{\text{macro}}\) 与微观残余 \(\Psi = \rho^{\text{micro}}\) 满足：

\[
\Phi = \mathcal{F}(\Psi, \nabla \Phi|_{\partial}), \quad
\Psi = \mathcal{G}(\Phi, \nabla \Psi|_{\partial}),
\]

其中 \(\partial\) 表示因果断裂边界（如视界、奇点）。

### 定义 5（演化算子）

定义算子 \(\mathcal{T}\) 为：

\[
\mathcal{T}(\mathcal{I}) = \arg\min_{\mathcal{I}'} \mathcal{S}[\mathcal{I}']
\]

其中 \(\mathcal{S}\) 是作用量（见第六部分候选形式）。宇宙演化由梯度流给出：

\[
\frac{d\mathcal{I}}{dt} = - \nabla \mathcal{S}[\mathcal{I}].
\]

### 定理 2（不动点存在性）

若存在度量 \(d\) 使得 \(\mathcal{T}\) 是压缩映射：

\[
d(\mathcal{T}(\mathcal{I}_1), \mathcal{T}(\mathcal{I}_2)) \le k \, d(\mathcal{I}_1, \mathcal{I}_2), \quad k<1,
\]

则存在唯一不动点 \(\mathcal{I}^*\) 满足 \(\mathcal{I}^* = \mathcal{T}(\mathcal{I}^*)\)。该不动点对应宇宙的稳定态或吸引子。

**注释**：定理2是经典的 Banach 不动点定理的直接应用。压缩性需要根据具体函数空间验证（待数值模拟确认）。

---

## 第五部分：离散因果图动力系统（可计算形式）

定义三步更新规则，其中所有量均基于可干预因果容量。

### 步骤 1：因果传播

对每条边 \(u \to v\)，首先计算实时因果容量（通过模拟中的干预估计）：

\[
\mathcal{C}_{uv}^{t} = \frac{\delta v}{\delta(\mathrm{do}(u))} \quad \text{(根据模拟中的干预估计)}
\]

然后更新节点信息密度：

\[
\rho_v^{t+1} = (1-\eta)\rho_v^t + \eta \sum_{u \in \text{Pa}(v)} \rho_u^t \cdot \mathcal{C}_{uv}^t
\]

其中 \(0<\eta<1\) 为传播速率（无量纲），\(\mathcal{C}_{uv}^t\) 归一化使得 \(\sum_{u} \mathcal{C}_{uv}^t = 1\)（对固定 \(v\)）。

### 步骤 2：宏微观分解

计算局部平均场：

\[
\bar{\rho}_v = \frac{1}{|\mathcal{N}(v)|} \sum_{u \in \mathcal{N}(v)} \rho_u,
\]

其中 \(\mathcal{N}(v)\) 为 \(v\) 的邻居（入边与出边）。定义：

\[
\rho_v^{\text{macro}} = \sigma(\rho_v - \bar{\rho}_v),\quad
\rho_v^{\text{micro}} = \rho_v - \rho_v^{\text{macro}},
\]

\(\sigma\) 为非线性激活函数（如 sigmoid 或阈值函数）。

### 步骤 3：总量重分配与拓扑重连

- 强制约束：

  \[
  \sum_v \rho_v^{\text{macro}} + \lambda \sum_v \rho_v^{\text{micro}} = C,
  \]

  通过全局缩放调整 \(\rho_v^{\text{micro}}\) 实现。

- 边重连概率基于因果容量：

  \[
  P(u\to v) = \operatorname{softmax}\left( \alpha \rho_u \rho_v + \beta |\mathcal{C}_{uv} - \bar{\mathcal{C}}| \right),
  \]

  删除容量 \(\mathcal{C}_{uv} < \epsilon\) 的边，以概率 \(P\) 创建新边，新边的初始容量由干预响应估计给出。

### 定理 3（离散系统收敛性）

在上述规则下，若 \(\lambda, \eta\) 满足适当条件（例如 \(\eta<1/(\max\text{入度})\)），则 \(\mathcal{I}_t\) 收敛到不动点附近的有界震荡。

**证明**：基于 Banach 不动点定理应用于复合算子，需验证压缩性条件（见数值模拟部分）。

---

## 第六部分：连续极限——核心开放问题

### 开放问题 1（连续极限的存在性）

当前框架中最关键且未解决的问题是：**在何种条件下，离散因果图序列 \(\Gamma_n\) 可以收敛到一个光滑洛伦兹流形 \(\mathcal{M}\)？** 具体地，需要证明：

1. **局域性涌现**：存在 \(\epsilon>0\) 使得图中距离 \(<\epsilon\) 的节点可嵌入到 \(\mathbb{R}^d\) 的邻域，且邻域结构稳定。
2. **维数稳定**：存在整数 \(d\) 使得大多数节点的局部度（邻居数）满足 \(\deg(v) \sim O(\epsilon^{-d})\)。
3. **洛伦兹符号**：因果容量张量 \(\mathcal{C}_{\mu\nu}(x)\) 的本征值符号为 \((-,+,+,+)\) 或等价地，传播方向区分类时与类空。
4. **光锥稳定性**：因果容量在微扰下保持光锥结构不变。

**状态**：这是一个开放问题，也是所有预几何理论共同面临的挑战。以下内容**假设**连续极限存在，并探索其有效描述。

---

### 假设 1（连续化条件）

当 \(|V|\to\infty\)，平均连接尺度 \(\ell\to 0\)，且 \(\rho(v) \sim O(\ell^{-d})\)（\(d\) 为维数），因果图可嵌入到微分流形 \(\mathcal{M}\)，节点 \(v\) 映射为 \(x\in\mathcal{M}\)，边变为局域因果锥结构，因果容量成为平滑函数 \(\mathcal{C}(x,y)\) 并进一步退化为张量场 \(\mathcal{C}_{\mu\nu}(x)\)。

### 定义 6（场变量）

\[
\rho(x) = \lim_{\ell\to 0} \ell^d \rho_v,\quad
\Phi(x) = \mathcal{C}[\rho(x)],\quad
\Psi(x) = \rho(x) - \Phi(x).
\]

宏观场 \(\Phi\) 为 \(\rho\) 的低通滤波（截止波数 \(k_c\)），微观场 \(\Psi\) 为高通分量。因果容量场在连续极限下需要张量化：定义因果容量张量 \(\mathcal{C}_{\mu\nu}(x)\)，量纲同度规扰动。

### 定理 4（因果传播方程的连续极限）

在连续极限存在的假设下，离散传播规则在连续极限下变为守恒型 PDE：

\[
\partial_t \rho + \nabla \cdot J_c = S,
\quad J_c = -D \nabla \rho + A(\Phi,\Psi),
\]

其中 \(D\) 为扩散系数（量纲 \(L^2 T^{-1}\)），\(A\) 为宏观因果驱动项，\(S\) 为拓扑变化源项（来自重连）。同时，因果流 \(J_c^\mu\) 满足：

\[
\partial_\mu J_c^\mu = 0, \quad J_c^\mu = (\rho, \vec{J}_c).
\]

**证明**：通过 Chapman–Enskog 展开或流体动力学极限，从步骤1–3推导得到（在连续极限存在的前提下）。

### 公理 6（作用量原理——候选形式）

为封闭连续理论，我们假设存在一个作用量泛函 \(\mathcal{S}[\Phi,\Psi,\mathcal{C}_{\mu\nu}]\)，其变分给出场方程。在标量近似下，候选形式为：

\[
\mathcal{S}_{\text{cand}}[\Phi,\Psi,\mathcal{C}] = \int d^4x \left[ (\partial \Phi)^2 + (\nabla \Psi)^2 + \lambda \Phi \langle \Psi^2 \rangle + V(\Phi) + \frac{1}{2} (\nabla_\mu \mathcal{C})^2 \right].
\]

更一般地，因果容量张量场 \(\mathcal{C}_{\mu\nu}\) 的动力学由下式给出：

\[
\mathcal{S}_{\mathcal{C}} = \int d^4x \sqrt{-g} \left( \frac{1}{2} \nabla^\alpha \mathcal{C}_{\mu\nu} \nabla_\alpha \mathcal{C}^{\mu\nu} + \frac{1}{2} m_c^2 \mathcal{C}_{\mu\nu}\mathcal{C}^{\mu\nu} + J^{\mu\nu}\mathcal{C}_{\mu\nu} \right),
\]

其中 \(J^{\mu\nu}\) 是由 \(\rho,\Psi\) 构成的源项。这保证了因果容量本身是一个动力学场，而非仅统计关联。

**注释**：公理6是一个**候选**的低能有效作用量，并非从离散图推导而来。其合理性需通过数值模拟检验连续极限下的普适类。

### 定理 5（变分方程——候选）

在候选作用量下，变分给出：
- 对 \(\Phi\) 变分：\(\Box \Phi = \lambda \langle \Psi^2 \rangle + \frac{dV}{d\Phi}\)。
- 对 \(\Psi\) 变分（高斯近似下）：\(\Box \Psi + m_{\text{eff}}^2(\Phi) \Psi = 0\)，其中 \(m_{\text{eff}}^2 = \frac{\delta V}{\delta \Psi^2}\)。
- 对 \(\mathcal{C}_{\mu\nu}\) 变分：\(\Box \mathcal{C}_{\mu\nu} + m_c^2 \mathcal{C}_{\mu\nu} = J_{\mu\nu}\)。

**证明**：直接计算变分导数。

---

感谢指正，这确实是关键漏洞。下面给出修正后的**第七部分**及相应调整，彻底移除背景闵可夫斯基度规，让洛伦兹结构成为动力学涌现的候选结果。

---

## 第七部分：几何涌现——从传播不对称性到有效度规（候选机制）

### 开放问题 1（续）：洛伦兹符号的涌现

连续极限存在的必要条件之一是因果容量张量 \(\mathcal{C}_{\mu\nu}(x)\) 的本征值符号呈现 \(( - , + , + , + )\) 或等价地，传播方向区分出类时与类空。**目前尚未证明该符号结构必然从离散动力学中产生**。以下内容为候选机制：定义有效度规为因果容量张量的某个函数，其符号需由动力学自洽决定。

### 定义 7（候选有效度规）

设 \(\mathcal{C}_{\mu\nu}(x)\) 为连续极限下从因果图涌现的二阶对称张量场。定义有效度规为：

\[
g_{\mu\nu}(x) \;=\; \phi(x) \; \mathcal{C}_{\mu\nu}(x)
\]

其中 \(\phi(x)\) 是一个标量因子，其作用是调整整体符号使得 \(\det(g_{\mu\nu}) < 0\)（洛伦兹符号）。更一般地，可考虑逆矩阵形式：

\[
g^{\mu\nu}(x) \;=\; \psi(x) \; \mathcal{C}^{\mu\nu}(x)
\]

并约定物理度规 \(g_{\mu\nu}\) 为 \(g^{\mu\nu}\) 的逆。两种定义的差异可通过场重定义吸收。

**关键点**：这里没有任何背景 \(\eta_{\mu\nu}\)。洛伦兹符号（三个正、一个负）必须作为因果容量张量本征值谱的动力学结果而出现。在数值模拟中，可通过计算 \(\mathcal{C}_{\mu\nu}\) 的本征值符号来检验是否涌现洛伦兹结构。

### 物理解释

因果容量张量 \(\mathcal{C}_{\mu\nu}\) 的物理意义：它度量沿方向 \(dx^\mu\) 传播信息的“效率”。若在某方向传播效率为负（即干预导致反向响应），则该方向为类时方向（时间流向）；若传播效率为正，则为类空方向。符号的差异来源于因果图的不对称性（出边与入边的统计差异）。因此，洛伦兹符号自然地对应于**传播效率的正负号分布**。

### 候选作用量中的度规替代

在连续极限下的候选作用量（公理6）中，所有导数 \(\partial_\mu\) 和体积元应当由有效度规 \(g_{\mu\nu}(\mathcal{C})\) 决定，而非预先存在的 \(\eta_{\mu\nu}\)。因此，作用量实际上是 \(\mathcal{C}_{\mu\nu}\) 和 \(\Phi,\Psi\) 的泛函，其中 \(g_{\mu\nu}\) 由定义7隐式定义。这形成了自洽方程：度规由因果容量决定，而因果容量的动力学又依赖于曲率。这一自指结构正是公理5（因果自指）的连续版本。

### 开放问题 2（爱因斯坦方程涌现）

在没有预设爱因斯坦-希尔伯特项的条件下，是否可以从作用量 \(\mathcal{S}[\mathcal{C}_{\mu\nu}, \Phi, \Psi]\) 对 \(g_{\mu\nu}(\mathcal{C})\) 的变分推导出广义相对论？目前这是一个开放猜想。

**猜想 0（引力涌现）**：在连续极限下，由因果图动力学导出的有效作用量在低能展开中自动包含爱因斯坦-希尔伯特项：

\[
\mathcal{S}_{\text{eff}}[\mathcal{C}] \;=\; \int d^4x \, \sqrt{-\det g(\mathcal{C})} \left( \frac{1}{16\pi G} R(g) + \cdots \right)
\]

其中 \(G \sim 1/\sqrt{N}\)（见第十一部分）。**状态**：尚需数值模拟或解析重整化群验证。

---

### 修正后对后续部分的影响

原定理6（扩展爱因斯坦方程）已删除，因为它预设了 \(R(g)\) 项。取而代之的是**开放问题2**和**猜想0**。相应调整第七部分标题为“几何涌现——传播不对称性到有效度规（候选机制）”，明确所有几何内容均为候选，非推导结果。

---

## 第八部分：量子结构与费米统计的涌现

### 定理 7（路径积分表示）

将 \(\Psi\) 视为量子涨落，定义配分函数：

\[
Z = \int \mathcal{D}\Psi \, e^{i \mathcal{S}_{\text{total}}[\Phi,\Psi,\mathcal{C}_{\mu\nu}]}.
\]

积分掉 \(\Psi\) 得到有效作用量：

\[
S_{\text{eff}}[\Phi,\mathcal{C}] = \int d^4x \sqrt{-g} \left[ R + V(\Phi) + \frac{1}{2}(\nabla\mathcal{C})^2 + \hbar_{\text{eff}} \log \det(\Box_g + m^2) \right],
\]

其中 \(\hbar_{\text{eff}} \sim \operatorname{Var}(\Psi)\)，量纲为作用量。

**证明**：高斯积分与行列式公式。

### 定理 8（费米统计的产生——候选机制）

因果路径不可重入性（信息排斥原理）强制路径空间的反对称性。设 \(\Psi^a(x)\) 为不同因果通道的幅，则不可区分路径的交换导致：

\[
\Psi^a(x)\Psi^b(y) = - \Psi^b(y)\Psi^a(x).
\]

因此 \(\Psi\) 取值于 Grassmann 代数，自然获得费米统计。**证明概要**：从路径积分中的反对称化构造，详见附录A。注意该论证依赖于“不可区分路径交换反对称”的额外假设，尚未从公理中推导。

### 猜想 1（狄拉克方程的候选唯一性）

在连续极限下，保持因果性、洛伦兹对称性和反对称代数的最简线性方程是狄拉克方程：

\[
(i\gamma^\mu D_\mu - m)\psi = 0.
\]

**状态**：这是一个候选机制，而非严格定理。完整证明需要 Clifford 代数、旋量结构与表示论。目前可说明在给定假设下狄拉克形式是自然候选。

---

## 第九部分：规范对称性的涌现（结构性猜想）

### 定义 8（因果纤维束）

在每个时空点 \(x\)，微观场取值于纤维 \(\mathcal{F}_x \cong \mathbb{C}^n\)，其中 \(n\) 是因果路径通道数。纤维上的变换：

\[
\Psi^a(x) \to U^a_{\;b}(x) \Psi^b(x),\quad U(x) \in GL(n,\mathbb{C}).
\]

因果容量 \(\mathcal{C}_{\mu\nu}(x)\) 在变换下保持不变（因其为可观测物理量）。

### 公理 8（信息范数守恒）

因果信息总量守恒要求内积 \(\langle \Psi|\Psi\rangle\) 不变，这迫使变换群从 \(GL(n,\mathbb{C})\) 约化到 \(U(n)\)。

### 定理 9（规范连接）

为使导数 \(D_\mu \Psi\) 在局域变换下协变，必须引入连接 \(A_\mu\)：

\[
D_\mu = \partial_\mu + A_\mu,\quad A_\mu \in \mathfrak{u}(n).
\]

规范场动力学来自有效作用量中的 \(\log\det\) 项展开：

\[
\log \det(\Box_g + m^2) \supset c \int \operatorname{Tr}(F_{\mu\nu}F^{\mu\nu}) + \cdots,
\]

其中 \(F_{\mu\nu} = [D_\mu,D_\nu]\)。

**证明**：标准规范原理与背景场方法。

### 猜想 2（规范群分解）

从 \(U(n)\) 出发，考虑因果路径稳定性与最小反作用能量原理，在某些结构假设下（如路径闭合拓扑、三色闭环、二态翻转等），稳定规范群的候选为 \(SU(3)\times SU(2)\times U(1)\)。  
**状态**：这是一个结构动机，而非唯一推导。目前只能说在特定稳定性条件与纤维分解假设下，该群是自然候选。完整数学证明尚未完成。

---

## 第十部分：粒子谱与基本常数的候选机制（远期猜想）

以下内容均为结构性猜想，远未达到数学严格性。它们提供了数值模拟可检验的定性预测，但不作为框架的核心主张。

### 猜想 3（粒子为稳定路径模）

粒子对应因果图中的稳定闭合路径类（同伦类）的驻波模式，路径作用量 \(S_c[\gamma] = \oint (-\log\mathcal{C}\, ds + \rho\, d\ell)\) 的变分为零。

### 猜想 4（三代结构）

按递归深度分层：第0层（无回返）→ 第一代，第1层（一次回返）→ 第二代，第2层（双重自反馈）→ 第三代。更高阶路径的概率呈指数衰减，因此前三代最稳定。

### 猜想 5（质量层级）

有效质量 \(m_i = m_0 \, e^{\alpha S_c[\gamma_i]}\)，因 \(S_c[\gamma^{(0)}] < S_c[\gamma^{(1)}] < S_c[\gamma^{(2)}]\)，得到 \(m_1 \ll m_2 \ll m_3\)。

### 猜想 6（电荷分数化）

稳定闭环的绕数 \(w(\gamma_i) \in \mathbb{Z}\) 与路径内对称阶数 \(N_i\) 给出电荷候选公式 \(Q_i = w(\gamma_i)/N_i\)。信息单值性条件要求绕数整数。取 \(w=\pm1,\pm2\)，\(N=3\) 得夸克分数电荷 \(\pm\frac13,\pm\frac23\)；轻子 \(N=1\) 得 \(Q=0,\pm1\)。**状态**：拓扑动机，缺严格同调框架。

---

## 第十一部分：基本常数与引力弱化（可推导结果）

### 定理 10（引力耦合常数的来源）

引力由宏观场 \(\Phi\) 和因果容量张量场 \(\mathcal{C}_{\mu\nu}\) 共同生成。因果容量的涨落方差决定了时空的响应：

\[
g_{\mu\nu} = \eta_{\mu\nu} + \frac{\kappa}{\sqrt{N}} \sum_{i=1}^N T^{(i)}_{\mu\nu} \cdot \frac{1}{\mathcal{C}_i},
\]

其中 \(N = |V|\) 是因果图总节点数。因此引力耦合常数正比于 \(1/\sqrt{N}\)，宏观系统 \(N\) 巨大导致引力极弱。

**证明**：从定义7和公理3，宏观场是 \(N\) 个独立微观贡献的和，标准差 \(\sim \sqrt{N}\)，且因果容量 \(\mathcal{C}_i\) 的典型值 \(\sim 1\)，故相对耦合强度 \(\sim 1/\sqrt{N}\)。

### 定理 11（有效普朗克常数）

\[
\hbar_{\text{eff}} = \frac{\operatorname{Var}(\Psi)}{\int \langle \Psi^2 \rangle} \sim \frac{1}{N},
\]

宏观极限下量子效应减弱。

**证明**：由中心极限定理与微观涨落的方差定义。

---

## 第十二部分：可验证性——数值模拟预测

为了将框架从候选理论转化为可检验的物理，必须执行以下数值实验：

### 预测 1（离散因果图模拟）

对随机初始化的小规模因果图（\(|V| \sim 10^3-10^4\)）运行第五部分的离散动力系统，应观察到：
- 自发出现稳定路径模的分层结构（可能对应三代候选），
- 统计上出现规范群 \(U(n)\) 的残余对称性，其约化模式可检验猜想2，
- 质量比近似为指数形式。

### 预测 2（连续极限与洛伦兹结构）

通过大规模数值模拟，检验连续极限是否存在：
- 测量局部邻域维数 \(d\) 的稳定性，
- 计算因果容量张量的本征值符号，检查是否出现洛伦兹符号，
- 检验因果流 \(J_c^\mu\) 的守恒性。

### 预测 3（新效应）

- **第四代费米子缺失**：高阶路径指数失稳，预测不存在第四代轻子/夸克（若三代结构成立）。
- **中微子质量上限**：由 \(S_c[\gamma_\nu] > 0\) 极小，预测总和 \(\sum m_\nu < 0.1\,\text{eV}\)。
- **引力在超大尺度修正**：有限 \(N\) 导致引力有效耦合常数在宇宙尺度上产生可观测偏差。

---

## 第十三部分：结论与当前进展

本框架从五个公理出发：
1. 可干预因果容量基底（公理1），
2. 因果信息态（公理2），
3. 信息总量有限（公理3），
4. 宏观+微观总量约束（公理4），
5. 因果自指（公理5）。

**已数学成立的部分**：
- 可干预因果容量作为基本可观测量的定义（响应函数/传播子）
- 离散因果图动力系统（定义1–5，定理1–3）
- 尺度分解与粗化（定义1,2，推论1）
- 信息守恒律（定理1）
- 不动点存在性（定理2）
- 离散系统收敛性（定理3）
- 连续极限下的候选场方程（定理4,5，假设1，公理6）
- 路径积分表示与费米统计候选（定理7,8）
- 规范连接与Yang-Mills项（定理9）
- 引力弱化与有效普朗克常数（定理10,11）

**核心开放问题与猜想**：
- **开放问题1**：连续极限的存在性（洛伦兹结构、维数、光锥）
- **开放问题2**：爱因斯坦方程是否从图动力学涌现（猜想0）
- 规范群分解为 \(SU(3)\times SU(2)\times U(1)\)（猜想2）
- 粒子三代结构（猜想3–6）
- 狄拉克方程的唯一性（猜想1）

**可验证预测**：
- 离散因果图模拟应出现稳定的普适结构
- 洛伦兹符号与光锥的数值检测
- 第四代费米子缺失、中微子质量上限、超大尺度引力修正

本框架的核心原创洞察——**因果作为可干预的传输能力（因果容量/传播子）**——是连接信息、几何与动力学的统一语言。下一步的关键是**数值模拟**（第五部分与预测1）以及**连续极限的严格分析**（第六部分），特别是因果容量张量动力学与洛伦兹结构涌现的实现。所有参数原则上可由因果图的基本性质（干预响应估计）确定或通过数值模拟校准。

---

## 附录A：费米统计与狄拉克候选方程（概要）

从Grassmann代数与因果传播的线性化条件出发，要求方程在洛伦兹变换下协变且保持概率流守恒，最一般形式为 \((i\Gamma^\mu D_\mu - m)\psi=0\)。进一步要求满足Clifford代数 \(\{\Gamma^\mu,\Gamma^\nu\}=2g^{\mu\nu}\)，则 \(\Gamma^\mu\) 必须为狄拉克矩阵。这给出了狄拉克方程作为候选。但完整唯一性证明需要附加的表示论与旋量结构假设，目前尚未严格完成。

## 附录B：因果容量的数值估计方法（可操作指南）

在离散因果图模拟中，对于每条边 \(u\to v\)，因果容量 \(\mathcal{C}_{uv}\) 通过主动干预方式估计：
1. 固定节点 \(u\) 的所有其他输入，对 \(u\) 施加一个已知变化 \(\delta u\)（例如增加 \(\epsilon\)），
2. 测量节点 \(v\) 在下一时间步的响应 \(\delta v\)（排除其他共同原因），
3. 计算比率 \(|\delta v| / |\delta u|\)，多次独立干预取平均，
4. 若图结构允许，可使用工具变量或后门准则调整混淆因子。

该方法在模拟中可直接观测并用于更新规则，使得整个系统完全自洽且可验证。该定义与线性响应理论中的**响应函数**、**格林函数**以及因果推断中的**干预效果**严格一致。

## 附录C：与现有理论的关系

本框架可视为以下思想的交汇：
- **因果集理论**：图结构作为基底，但这里边带有可干预容量。
- **张量网络与纠缠几何**：传播能力类比于纠缠熵决定几何。
- **诱导引力与熵引力**：宏观场 \(\Phi\) 类似于热力学变量，引力来自统计涨落。
- **非平衡统计力学**：传播+重连规则构成非平衡稳态。
- **类比引力**：传播阻抗作为光学度规。

不同点在于：**可干预因果容量**直接连接操作定义与传播子，避免了互信息的歧义性。连续极限的存在性仍是最大挑战，但本框架提供了可数值检验的离散基底。